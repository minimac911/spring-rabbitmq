version: '3.1'
services:
  rabbitmq:
    build: ./rabbitmq
    container_name: spring-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - data:/var/lib/rabbitmq/mnesia

volumes:
  data:
    